# No subir NUNCA el archivo con la clave de la base de datos
api/config.php
**/config.php

# Estado del desplegador FTP (se genera solo)
.ftp-deploy-sync-state.json

# Basura del sistema
.DS_Store
Thumbs.db
*.log
node_modules/
